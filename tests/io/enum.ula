enum Color
    Red
    White

x: Color = Color.White
y: Color = Color.Red
mostrar(x == Color.White)
mostrar(x == Color.Red)
mostrar(x == y)

# Testing enums as parameters and return values
def giveMeSomeColor(some: Color) -> Color
    retorna some

cir: Color = Color.Red
xy = giveMeSomeColor(cir)
mostrar(xy == Color.Red)
mostrar(xy es Color)